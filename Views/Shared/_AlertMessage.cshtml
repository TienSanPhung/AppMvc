@{
    var message = TempData["StatusMessage"] as string;
}
@if(!string.IsNullOrEmpty(message)){
    <div class="alert alert-dismissible alert-warning shadow" role="alert">
        @message
        <button class="btn-close" type="button" data-dismis="alert" aria-label="Close">
        </button>

    </div>
}